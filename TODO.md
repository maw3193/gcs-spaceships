To-Do list
===

* Replace with github issues.
* Have some way to specify identifiers that are implicitly present, like Math, self and entity.
* More explicitly separate library-provided identifiers in code snippets, so it can clean up orphans.
* Consider multiple variable declarations, e.g. `var foo=1, bar=2`
